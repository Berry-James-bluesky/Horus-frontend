_Simple button component for adding a timer_


### Props:

`addTimer: any` = Function the button will run on press

`isDisabled: boolean` = Pass as `true` until all form inputs are filled out properly.